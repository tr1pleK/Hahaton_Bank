# Рефакторинг алгоритма прогнозирования финансов

## Проблема

Для новых пользователей (без транзакций) алгоритм возвращал жестко закодированные значения:
- `salary_day: 25`
- `salary_amount: 34800`
- `rent_day: 1`
- `rent_amount: 6500`

Это приводило к некорректным прогнозам для пользователей, у которых еще нет достаточного количества транзакций для определения паттернов.

## Решение

### 1. Улучшена функция `detect_fixed_events`

**Изменения:**
- Убраны жестко закодированные значения по умолчанию
- Добавлена проверка минимального количества транзакций (минимум 5 для начала анализа)
- Улучшена логика определения зарплаты:
  - Ищет регулярные крупные поступления (>20000)
  - Требует минимум 2 повторения для определения паттерна
  - Сначала ищет в диапазоне 20-30 числа (типичный период зарплаты)
  - Если не найдено, расширяет поиск
- Улучшена логика определения аренды:
  - Ищет транзакции с категорией "Rent"
  - Или крупные расходы (>2000) в начале месяца (1-10 число)
  - Требует минимум 2 повторения для определения паттерна
- Функция теперь возвращает 6 значений вместо 4:
  - `salary_day, salary_amount, rent_day, rent_amount, salary_detected, rent_detected`

**Результат:** Для новых пользователей возвращаются `None` значения вместо предположений.

### 2. Улучшена функция `compute_spending_stats`

**Изменения:**
- Для пустого DataFrame возвращает нулевые значения вместо предположений (-500)
- Улучшен расчет средних ежедневных расходов на основе реальных данных
- Использует временные интервалы между транзакциями для более точного расчета

**Результат:** Для новых пользователей не делаются предположения о расходах.

### 3. Обновлена функция `forecast_to_month_end`

**Изменения:**
- Принимает `Optional[int]` и `Optional[float]` для зарплаты и аренды
- Проверяет наличие значений перед использованием
- Если зарплата/аренда не определены, использует только средние ежедневные значения

**Результат:** Прогноз для новых пользователей основан только на текущем балансе без предположений о регулярных платежах.

### 4. Обновлена функция `assess_budget_stability`

**Изменения:**
- Принимает `Optional[float]` для зарплаты и аренды
- Возвращает 0.0, если данные не определены

**Результат:** Для новых пользователей устойчивость бюджета не рассчитывается.

### 5. Обновлена функция `estimate_financial_pillow`

**Изменения:**
- Принимает `Optional[int]` для `salary_day`
- Корректно обрабатывает случай, когда зарплата не определена

### 6. Обновлена функция `get_balance_forecast`

**Изменения:**
- Обрабатывает новые возвращаемые значения из `detect_fixed_events`
- Добавляет флаги `salary_detected` и `rent_detected` в ответ API
- Добавляет специальные рекомендации для новых пользователей:
  - Если нет транзакций - сообщение о необходимости добавить транзакции
  - Если недостаточно транзакций - сообщение о необходимости добавить больше данных
  - Если зарплата не определена - подсказка о добавлении крупных поступлений
  - Если аренда не определена - подсказка о добавлении крупных расходов в начале месяца

**Результат:** API теперь возвращает:
```json
{
  "summary": {
    "salary_day": null,  // вместо 25
    "salary_amount": null,  // вместо 34800
    "salary_detected": false,  // новый флаг
    "rent_day": null,  // вместо 1
    "rent_amount": null,  // вместо 6500
    "rent_detected": false,  // новый флаг
    ...
  },
  "recommendations": [
    "ℹ️ У вас пока нет транзакций. Прогноз будет доступен после добавления нескольких транзакций.",
    ...
  ]
}
```

## Критерии определения паттернов

### Зарплата определяется, если:
- Минимум 5 транзакций в истории
- Минимум 2 крупных поступления (>20000)
- Поступления происходят в один и тот же день месяца (или близкие дни)

### Аренда определяется, если:
- Минимум 5 транзакций в истории
- Минимум 2 транзакции с категорией "Rent" ИЛИ
- Минимум 2 крупных расхода (>2000) в начале месяца (1-10 число)
- Расходы происходят в один и тот же день месяца

## Примеры поведения

### Новый пользователь (0 транзакций):
```json
{
  "salary_day": null,
  "salary_amount": null,
  "salary_detected": false,
  "rent_day": null,
  "rent_amount": null,
  "rent_detected": false,
  "forecast_daily": [
    {
      "Date": "2025-12-07",
      "PredictedBalance": 0.0,  // только текущий баланс
      "Income": 0,
      "Expense": 0
    }
  ]
}
```

### Пользователь с 3 транзакциями:
```json
{
  "salary_day": null,  // недостаточно данных
  "salary_detected": false,
  "recommendations": [
    "ℹ️ У вас недостаточно транзакций для точного прогноза...",
    "💡 Система пока не определила день зарплаты..."
  ]
}
```

### Пользователь с достаточным количеством данных:
```json
{
  "salary_day": 25,  // определено на основе данных
  "salary_amount": 35000,
  "salary_detected": true,
  "rent_day": 1,
  "rent_amount": 6000,
  "rent_detected": true
}
```

## Преимущества

1. ✅ **Честность**: Не делаются предположения о финансах пользователя без данных
2. ✅ **Точность**: Прогнозы основаны только на реальных данных
3. ✅ **Прозрачность**: Флаги `salary_detected` и `rent_detected` показывают, насколько надежен прогноз
4. ✅ **Полезность**: Рекомендации помогают пользователю понять, что нужно для улучшения прогноза
5. ✅ **Адаптивность**: Алгоритм автоматически определяет паттерны по мере накопления данных

## Обратная совместимость

Изменения обратно совместимы:
- Для пользователей с достаточным количеством данных поведение не изменилось
- Для новых пользователей прогноз стал более консервативным и честным
- API возвращает дополнительные поля, но старые клиенты могут их игнорировать

