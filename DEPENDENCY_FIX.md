# 🔧 Исправление конфликта зависимостей

## Проблема

При сборке Docker образа возникает ошибка:

```
ERROR: Cannot install numpy==2.0.0 because these package versions have conflicting dependencies.
The conflict is caused by:
    pandas 2.1.3 depends on numpy<2 and >=1.23.2
```

## Причина

- Модель была обучена с **numpy 2.0.0** (использует `numpy._core.numeric`)
- **pandas 2.1.3** не поддерживает numpy 2.0.0 (требует `numpy<2`)
- **scikit-learn 1.7.2** поддерживает numpy 2.0.0

## ✅ Решение

### Обновлен `requirements.txt`:

- `scikit-learn==1.7.2` - совместимо с моделью
- `pandas>=2.2.0` - поддерживает numpy 2.0.0
- `numpy>=2.0.0` - требуется для загрузки модели
- `scipy>=1.13.0` - совместимо с numpy 2.0.0

### Пересоберите Docker образ:

```bash
docker-compose down
docker-compose build --no-cache
docker-compose up
```

## Альтернативное решение

Если pandas 2.2.0+ вызывает проблемы, переобучите модель с numpy 1.x:

1. **Установите совместимые версии локально:**
   ```bash
   pip install scikit-learn==1.7.2 numpy==1.26.2 pandas==2.1.3 scipy==1.11.4
   ```

2. **Переобучите модель:**
   ```bash
   python scripts/train_transaction_classifier.py "путь/к/ci_data.csv"
   ```

3. **Обновите `requirements.txt`:**
   ```txt
   scikit-learn==1.7.2
   pandas==2.1.3
   numpy==1.26.2
   scipy==1.11.4
   ```

4. **Пересоберите Docker:**
   ```bash
   docker-compose build --no-cache
   ```

## Проверка

После успешной сборки в логах должно появиться:

```
✅ Все компоненты модели загружены успешно
   is_trained: True
✅ Модель успешно загружена и готова к использованию
```

