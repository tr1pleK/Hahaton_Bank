# 🔍 Диагностика проблемы с вероятностью 0.5

## Проблема

Модель всегда возвращает вероятность 0.5, что означает использование fallback классификации вместо обученной модели.

## Возможные причины

1. **Модель не найдена по указанному пути**
2. **Ошибка при загрузке модели**
3. **Модель загружена, но не помечена как обученная**
4. **Отсутствуют компоненты модели (vectorizer, scaler)**

## Диагностика

### Шаг 1: Проверьте логи сервера

При запуске сервера и выполнении запросов вы должны видеть сообщения:

```
🔍 Найдена модель по пути: C:\Users\Егор\IdeaProjects\Hahaton_Bank\backend\ml_models\transaction_classifier.pkl
📂 Загрузка модели из: ...
✅ Все компоненты модели загружены успешно
   is_trained: True
```

Если видите:
```
⚠️ Модель не найдена по пути: ...
```
или
```
❌ Ошибка при загрузке модели: ...
```

То проблема в загрузке модели.

### Шаг 2: Проверьте существование файла модели

Убедитесь, что файл существует:
```bash
# Windows PowerShell
Test-Path "C:\Users\Егор\IdeaProjects\Hahaton_Bank\backend\ml_models\transaction_classifier.pkl"

# Или просто проверьте в проводнике
```

### Шаг 3: Проверьте содержимое файла модели

Попробуйте загрузить модель вручную:

```python
import pickle
import os

model_path = r"C:\Users\Егор\IdeaProjects\Hahaton_Bank\backend\ml_models\transaction_classifier.pkl"

if os.path.exists(model_path):
    with open(model_path, 'rb') as f:
        model_data = pickle.load(f)
    
    print("Ключи в файле:", model_data.keys())
    print("is_trained:", model_data.get('is_trained'))
    print("model:", type(model_data.get('model')))
    print("vectorizer:", type(model_data.get('vectorizer')))
    print("scaler:", type(model_data.get('scaler')))
else:
    print("Файл не найден!")
```

### Шаг 4: Переобучите модель

Если модель повреждена или неполная, переобучите её:

```bash
python scripts/train_transaction_classifier.py "c:\Users\Егор\Downloads\Telegram Desktop\ci_data.csv"
```

## Решения

### Решение 1: Проверьте путь к модели

Убедитесь, что путь правильный. В коде используется:
```python
default_path = r"C:\Users\Егор\IdeaProjects\Hahaton_Bank\backend\ml_models\transaction_classifier.pkl"
```

### Решение 2: Переобучите модель

Если модель не загружается, переобучите её заново.

### Решение 3: Проверьте формат сохранения

Убедитесь, что модель сохраняется со всеми компонентами:
- model (RandomForestClassifier)
- vectorizer (TfidfVectorizer)
- scaler (StandardScaler)
- is_trained (True)

## Что делать дальше

1. **Запустите сервер** и посмотрите логи при старте
2. **Выполните тестовый запрос** и посмотрите логи
3. **Проверьте файл модели** на существование
4. **Если модель не найдена** - переобучите её
5. **Если модель найдена, но не загружается** - проверьте содержимое файла

## Ожидаемое поведение

После правильной загрузки модели вы должны видеть:
- Вероятности от 0.0 до 1.0 (не всегда 0.5)
- Разные вероятности для разных транзакций
- Логи показывают "✅ Модель успешно загружена и готова к использованию"

