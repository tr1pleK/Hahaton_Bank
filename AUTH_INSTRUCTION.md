# 📘 Инструкция по авторизации в API

## 🚀 Быстрый старт

### Шаг 1: Убедитесь, что сервер запущен

Сервер должен быть доступен по адресу (обычно):
```
http://localhost:8000
```

### Шаг 2: Получите токен доступа

## 📋 Способы авторизации

### Способ 1: Через Swagger UI (Рекомендуется для тестирования)

**⚠️ ВАЖНО:** Swagger UI показывает OAuth2 форму, но наш API использует простой Bearer token. Следуйте инструкции ниже:

#### Шаг 1: Получите токен через `/auth/login`

1. **Откройте Swagger UI:**
   ```
   http://localhost:8000/docs
   ```

2. **Найдите endpoint `POST /auth/login`** в разделе `auth`

3. **Нажмите "Try it out"**

4. **Введите данные для входа:**
   ```json
   {
     "email": "demo@finance.app",
     "password": "demo123"
   }
   ```

5. **Нажмите "Execute"**

6. **Скопируйте значение `access_token` из ответа** (это длинная строка, начинающаяся с `eyJ...`)

#### Шаг 2: Авторизуйтесь в Swagger UI

**Вариант А: Прямая вставка токена (Рекомендуется)**

1. **Нажмите кнопку "Authorize" (🔒) в верхней части страницы**

2. **В модальном окне найдите поле для токена** (может быть внизу формы или в отдельной секции)

3. **Если видите форму OAuth2 с полями username/password:**
   - **НЕ заполняйте эти поля!** Они не работают с нашим API
   - Прокрутите вниз или найдите поле для прямого ввода токена
   - Вставьте **только токен** (без слова "Bearer")

4. **Если есть поле "Value" или "Token":**
   - Вставьте скопированный токен
   - **НЕ добавляйте "Bearer"** - Swagger добавит его автоматически

5. **Нажмите "Authorize"**

6. **Нажмите "Close"**

**Вариант Б: Если форма OAuth2 не работает**

Если модальное окно показывает только OAuth2 форму и не работает:

1. **Закройте модальное окно авторизации**

2. **Для каждого запроса вручную добавляйте заголовок:**
   - Откройте endpoint (например, `/transactions/predict-category`)
   - Нажмите "Try it out"
   - В разделе "Parameters" найдите или добавьте заголовок
   - Добавьте заголовок: `Authorization` со значением: `Bearer YOUR_TOKEN`

**Альтернативный способ - использовать ReDoc:**

1. Откройте: `http://localhost:8000/redoc`
2. ReDoc обычно лучше работает с Bearer токенами

---

### Способ 2: Через curl (Командная строка)

#### Шаг 1: Вход в систему

```bash
curl -X POST "http://localhost:8000/auth/login" \
  -H "Content-Type: application/json" \
  -d '{
    "email": "demo@finance.app",
    "password": "demo123"
  }'
```

**Ответ:**
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxIiwiZW1haWwiOiJkZW1vQGZpbmFuY2UuYXBwIiwiZXhwIjoxNzA1NzI4MDAwfQ.xxxxx",
  "token_type": "bearer"
}
```

#### Шаг 2: Сохраните токен в переменную

**Windows PowerShell:**
```powershell
$response = Invoke-RestMethod -Uri "http://localhost:8000/auth/login" -Method POST -ContentType "application/json" -Body '{"email":"demo@finance.app","password":"demo123"}'
$token = $response.access_token
Write-Host "Token: $token"
```

**Windows CMD:**
```cmd
# Используйте PowerShell или сохраните токен вручную
```

**Linux/Mac:**
```bash
TOKEN=$(curl -X POST "http://localhost:8000/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"email":"demo@finance.app","password":"demo123"}' | jq -r '.access_token')

echo "Token: $TOKEN"
```

#### Шаг 3: Используйте токен для запросов

```bash
curl -X POST "http://localhost:8000/transactions/predict-category" \
  -H "Authorization: Bearer YOUR_TOKEN_HERE" \
  -H "Content-Type: application/json" \
  -d '{
    "Date": "1/1/2023",
    "RefNo": "3.00E+11",
    "Withdrawal": 3950.0,
    "Deposit": 0.0,
    "Balance": 1837.23
  }'
```

---

### Способ 3: Через Postman

1. **Создайте новый запрос:**
   - Method: `POST`
   - URL: `http://localhost:8000/auth/login`

2. **Перейдите на вкладку "Body"**
   - Выберите "raw"
   - Выберите "JSON" в выпадающем списке

3. **Введите данные:**
   ```json
   {
     "email": "demo@finance.app",
     "password": "demo123"
   }
   ```

4. **Нажмите "Send"**

5. **Скопируйте `access_token` из ответа**

6. **Для использования токена:**
   - Перейдите на вкладку "Authorization"
   - Выберите тип: "Bearer Token"
   - Вставьте токен в поле "Token"
   - Теперь все запросы будут автоматически включать этот токен!

---

### Способ 4: Через Python (requests)

```python
import requests

# Шаг 1: Вход
login_url = "http://localhost:8000/auth/login"
login_data = {
    "email": "demo@finance.app",
    "password": "demo123"
}

response = requests.post(login_url, json=login_data)
token_data = response.json()
access_token = token_data["access_token"]

print(f"Token: {access_token}")

# Шаг 2: Использование токена
headers = {
    "Authorization": f"Bearer {access_token}",
    "Content-Type": "application/json"
}

# Пример запроса к защищенному endpoint
predict_url = "http://localhost:8000/transactions/predict-category"
predict_data = {
    "Date": "1/1/2023",
    "RefNo": "3.00E+11",
    "Withdrawal": 3950.0,
    "Deposit": 0.0,
    "Balance": 1837.23
}

response = requests.post(predict_url, json=predict_data, headers=headers)
print(response.json())
```

---

### Способ 5: Через JavaScript (fetch)

```javascript
// Шаг 1: Вход
async function login() {
  const response = await fetch('http://localhost:8000/auth/login', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      email: 'demo@finance.app',
      password: 'demo123'
    })
  });
  
  const data = await response.json();
  const token = data.access_token;
  
  console.log('Token:', token);
  
  // Сохраните токен (например, в localStorage)
  localStorage.setItem('access_token', token);
  
  return token;
}

// Шаг 2: Использование токена
async function predictCategory(token) {
  const response = await fetch('http://localhost:8000/transactions/predict-category', {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${token}`,
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      Date: "1/1/2023",
      RefNo: "3.00E+11",
      Withdrawal: 3950.0,
      Deposit: 0.0,
      Balance: 1837.23
    })
  });
  
  const result = await response.json();
  console.log(result);
  return result;
}

// Использование
login().then(token => {
  predictCategory(token);
});
```

---

## 🔑 Демонстрационные учетные данные

Если демо-пользователь не создан автоматически, используйте:

- **Email:** `demo@finance.app`
- **Пароль:** `demo123`

### Создание демо-пользователя вручную

Если пользователь не существует, выполните:

```bash
python scripts/create_demo_user.py
```

Или зарегистрируйте нового пользователя:

```bash
curl -X POST "http://localhost:8000/auth/register" \
  -H "Content-Type: application/json" \
  -d '{
    "email": "demo@finance.app",
    "password": "demo123",
    "full_name": "Демо Пользователь"
  }'
```

---

## ⚠️ Частые проблемы и решения

### Проблема 1: "401 Unauthorized"

**Причина:** Неверный токен или токен не передан

**Решение:**
- Убедитесь, что используете правильный формат: `Authorization: Bearer YOUR_TOKEN`
- Проверьте, что токен не истек (токены действительны 24 часа)
- Получите новый токен через `/auth/login`

### Проблема 2: "Неверный email или пароль"

**Причина:** Неверные учетные данные

**Решение:**
- Проверьте правильность email и пароля
- Убедитесь, что пользователь существует (создайте через `/auth/register`)

### Проблема 3: "Connection refused" или "Cannot connect"

**Причина:** Сервер не запущен

**Решение:**
- Убедитесь, что сервер запущен: `python run.py` или `uvicorn app.main:app --reload`
- Проверьте правильность URL (обычно `http://localhost:8000`)

### Проблема 4: В Swagger UI не работает авторизация

**Решение:**
1. Получите токен через `/auth/login` в Swagger UI
2. Скопируйте только значение токена (без слова "Bearer")
3. Нажмите кнопку "Authorize" (🔒)
4. Вставьте токен в поле "Value"
5. Нажмите "Authorize" и "Close"

---

## 📝 Формат заголовка Authorization

Всегда используйте следующий формат:

```
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

**Важно:**
- Слово "Bearer" должно быть с заглавной буквы
- После "Bearer" должен быть пробел
- Затем идет сам токен (без кавычек)

---

## 🔍 Проверка авторизации

Проверить, что авторизация работает, можно через endpoint:

```bash
curl -X GET "http://localhost:8000/auth/me" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

Если авторизация успешна, вы получите данные пользователя:
```json
{
  "id": 1,
  "email": "demo@finance.app",
  "full_name": "Демо Пользователь",
  "balance": 10000.0
}
```

---

## 📚 Дополнительная информация

- Полная документация API: `http://localhost:8000/docs` (Swagger UI)
- Альтернативная документация: `http://localhost:8000/redoc`
- Информация о здоровье сервера: `http://localhost:8000/health`

---

## 💡 Советы

1. **Сохраняйте токен** после получения, чтобы не запрашивать его каждый раз
2. **Токены действительны 24 часа** - после истечения получите новый
3. **Используйте Swagger UI** для быстрого тестирования - там авторизация работает автоматически
4. **Проверяйте формат заголовка** - `Bearer` с пробелом обязателен

