# 🔧 Исправление проблемы загрузки модели

## Проблема

Модель не загружается из-за несовместимости версий библиотек:

```
ModuleNotFoundError: No module named 'numpy._core.numeric'
InconsistentVersionWarning: Trying to unpickle estimator DecisionTreeClassifier from version 1.7.2 when using version 1.3.2
```

## Причина

Модель была обучена с:
- scikit-learn 1.7.2
- numpy 2.x (использует `numpy._core.numeric`)

А в Docker контейнере установлены:
- scikit-learn 1.3.2
- numpy 1.26.2

## ✅ Решение

### Вариант 1: Обновить версии библиотек (Рекомендуется)

1. **Обновлен `requirements.txt`** с совместимыми версиями:
   - scikit-learn==1.7.2
   - numpy==2.0.0
   - scipy==1.13.1

2. **Пересоберите Docker образ:**
   ```bash
   docker-compose down
   docker-compose build --no-cache
   docker-compose up
   ```

### Вариант 2: Переобучить модель с текущими версиями

Если не хотите обновлять библиотеки, переобучите модель:

1. **Убедитесь, что версии библиотек совпадают:**
   ```bash
   pip install scikit-learn==1.3.2 numpy==1.26.2
   ```

2. **Переобучите модель:**
   ```bash
   python scripts/train_transaction_classifier.py "путь/к/ci_data.csv"
   ```

3. **Перезапустите Docker:**
   ```bash
   docker-compose restart backend
   ```

### Вариант 3: Использовать совместимые версии

Если модель была обучена локально, используйте те же версии в Docker:

1. Проверьте версии локально:
   ```bash
   pip show scikit-learn numpy
   ```

2. Обновите `requirements.txt` с этими версиями

3. Пересоберите Docker образ

## Проверка

После исправления в логах должно появиться:

```
✅ Все компоненты модели загружены успешно
   is_trained: True
✅ Модель успешно загружена и готова к использованию
```

Вместо:
```
❌ Ошибка при загрузке модели: No module named 'numpy._core.numeric'
```

## Важно

- **Версии библиотек должны совпадать** между обучением и загрузкой модели
- **numpy 2.x** использует другую структуру модулей (`numpy._core`), несовместимую с numpy 1.x
- **scikit-learn 1.7.2** несовместим с 1.3.2 при загрузке моделей

