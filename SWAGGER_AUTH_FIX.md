# 🔧 Исправление авторизации в Swagger UI

## Проблема

При нажатии на кнопку "Authorize" (🔒) в Swagger UI открывается модальное окно с OAuth2 формой, которая требует:
- username
- password  
- client_id
- client_secret

Но наш API использует простой Bearer token, полученный через `/auth/login` с email/password.

## ✅ Решение

### Способ 1: Получить токен и вставить напрямую

1. **Получите токен:**
   - Откройте `POST /auth/login` в Swagger UI
   - Введите:
     ```json
     {
       "email": "demo@finance.app",
       "password": "demo123"
     }
     ```
   - Нажмите "Execute"
   - Скопируйте `access_token` из ответа

2. **Вставьте токен в форму авторизации:**
   - Нажмите "Authorize" (🔒)
   - В модальном окне **прокрутите вниз** до поля для токена
   - Вставьте токен (без "Bearer")
   - Нажмите "Authorize"

### Способ 2: Использовать ReDoc (Альтернатива)

ReDoc лучше работает с Bearer токенами:

1. Откройте: `http://localhost:8000/redoc`
2. Используйте ту же авторизацию через Bearer token

### Способ 3: Добавлять заголовок вручную для каждого запроса

Если авторизация через форму не работает:

1. Откройте нужный endpoint (например, `/transactions/predict-category`)
2. Нажмите "Try it out"
3. В разделе параметров найдите возможность добавить заголовок
4. Добавьте:
   - **Name:** `Authorization`
   - **Value:** `Bearer YOUR_TOKEN_HERE`

### Способ 4: Использовать curl или Postman

См. инструкцию в `AUTH_INSTRUCTION.md` для использования curl или Postman.

## 🔍 Проверка авторизации

После авторизации проверьте:

1. Откройте `GET /auth/me`
2. Нажмите "Try it out"
3. Нажмите "Execute"
4. Если получили данные пользователя - авторизация работает! ✅

## ⚠️ Важные замечания

- **НЕ используйте** поля username/password в OAuth2 форме - они не работают
- **НЕ заполняйте** client_id и client_secret - они не нужны
- Используйте **только токен**, полученный через `/auth/login`
- Токен действителен **24 часа** (по умолчанию)

## 🐛 Если ничего не помогает

1. Проверьте, что сервер запущен: `http://localhost:8000/health`
2. Убедитесь, что пользователь существует (создайте через `/auth/register`)
3. Попробуйте использовать Postman или curl (см. `AUTH_INSTRUCTION.md`)

