# 🐳 Настройка ML модели в Docker

## Проблема

При запуске приложения в Docker модель не загружается, потому что:
- Модель находится на хосте: `ml_models/transaction_classifier.pkl`
- Docker использует именованный volume, который создается пустым
- Путь к модели не учитывает Docker окружение

## ✅ Решение

### Изменения в docker-compose.yml

Изменен volume для монтирования папки `ml_models` с хоста:

```yaml
volumes:
  - .:/app
  - ./ml_models:/app/ml_models  # Монтируем папку с хоста
```

Теперь папка `ml_models` с хоста монтируется в контейнер.

### Изменения в коде

Обновлен метод `_get_default_model_path()` для работы в Docker и на хосте:
1. Сначала проверяет относительный путь (работает в Docker)
2. Затем проверяет абсолютный путь Windows (для локального запуска)
3. Возвращает относительный путь для сохранения новой модели

## 🚀 Использование

### Шаг 1: Убедитесь, что модель обучена

```bash
python scripts/train_transaction_classifier.py "путь/к/ci_data.csv"
```

Модель сохранится в `ml_models/transaction_classifier.pkl`

### Шаг 2: Перезапустите Docker контейнер

```bash
docker-compose down
docker-compose up --build
```

### Шаг 3: Проверьте логи

В логах должно появиться:
```
🔍 Найдена модель по пути: /app/ml_models/transaction_classifier.pkl
✅ Модель успешно загружена и готова к использованию
```

## 📝 Проверка

После перезапуска выполните тестовый запрос и проверьте логи:
- Должна загружаться модель (не fallback)
- Вероятность должна быть разной (не всегда 0.5)

## ⚠️ Важно

1. **Папка `ml_models` должна существовать** на хосте
2. **Файл `transaction_classifier.pkl` должен быть в папке** `ml_models/`
3. **После обучения модели** перезапустите Docker контейнер

## 🔧 Альтернативное решение (если не работает)

Если монтирование не работает, можно скопировать модель в образ:

1. Добавьте в Dockerfile:
```dockerfile
# Создаем папку для моделей
RUN mkdir -p /app/ml_models

# Копируем модель (если она существует)
COPY ml_models/transaction_classifier.pkl /app/ml_models/transaction_classifier.pkl || true
```

2. Пересоберите образ:
```bash
docker-compose build --no-cache
docker-compose up
```

Но лучше использовать volume монтирование, так как модель будет обновляться без пересборки образа.

