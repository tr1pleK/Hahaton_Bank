# Руководство по дообучению модели

## Обзор

Система автоматического дообучения модели на основе новых данных пользователей. Модель автоматически дообучается каждую неделю (каждый понедельник в 3:00) на основе транзакций всех пользователей за последние 7 дней.

## Как это работает

### 1. Автоматическое дообучение

- **Расписание**: Каждый понедельник в 3:00
- **Данные**: Транзакции всех пользователей за последние 7 дней
- **Процесс**:
  1. Сбор новых транзакций из БД
  2. Преобразование в формат для обучения (Date, Category, Withdrawal, Deposit, Balance)
  3. Объединение с оригинальными данными (если доступен CSV файл)
  4. Обучение модели на объединенных данных
  5. Сохранение обновленной модели

### 2. Ручной запуск дообучения

Вы можете запустить дообучение вручную через API endpoint:

```bash
POST /ml/retrain?days_back=7
Authorization: Bearer YOUR_TOKEN
```

**Параметры:**
- `days_back` (опционально, по умолчанию 7): Количество дней назад для выборки транзакций (1-365)
- `original_csv_path` (опционально): Путь к оригинальному CSV файлу для объединения данных

**Пример запроса:**
```bash
curl -X POST "http://localhost:8000/ml/retrain?days_back=7" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

**Ответ:**
```json
{
  "success": true,
  "message": "Модель успешно дообучена",
  "new_transactions_count": 150,
  "metrics": {
    "f1_weighted": 0.92,
    "f1_macro": 0.89,
    "train_size": 500,
    "test_size": 200,
    "total_samples": 700,
    "categories": ["Food", "Misc", "Rent", "Salary", "Shopping"]
  },
  "model_path": "app/ml/classifier_v2.pkl"
}
```

## Структура системы

### Файлы

1. **`app/ml/transaction_classifier.py`**
   - Класс `TransactionClassifier`
   - Метод `train()` - обучение модели на DataFrame
   - Использует логику из `cybergarden_ML.py`

2. **`app/services/model_retraining.py`**
   - Функция `export_transactions_to_dataframe()` - экспорт транзакций из БД
   - Функция `retrain_model()` - основная логика дообучения

3. **`app/api/model_retraining.py`**
   - Endpoint `/ml/retrain` для ручного запуска

4. **`app/main.py`**
   - Планировщик задач (APScheduler)
   - Автоматический запуск каждую неделю

## Преобразование данных

### Из БД в формат обучения

Транзакции из БД преобразуются в формат для обучения:

**БД формат:**
- `date` - дата транзакции
- `amount` - сумма
- `category` - категория (TransactionCategory enum)
- `is_income` - является ли доходом

**Формат для обучения:**
- `Date` - дата транзакции
- `Category` - категория модели (Food, Misc, Rent, Salary, Shopping)
- `Withdrawal` - сумма снятия (0 для доходов)
- `Deposit` - сумма пополнения (0 для расходов)
- `Balance` - баланс (вычисляется последовательно)

### Маппинг категорий

**Система → Модель:**
- `Продукты` → `Food`
- `Прочие расходы` → `Misc`
- `Коммунальные услуги` → `Rent`
- `Зарплата` → `Salary`
- `Одежда` → `Shopping`
- Остальные → `Misc`

## Настройка

### Изменение расписания

В `app/main.py` измените параметры `CronTrigger`:

```python
# Каждый понедельник в 3:00 (текущее)
trigger=CronTrigger(day_of_week='mon', hour=3, minute=0)

# Каждый день в 2:00
trigger=CronTrigger(hour=2, minute=0)

# Каждое воскресенье в 4:00
trigger=CronTrigger(day_of_week='sun', hour=4, minute=0)
```

### Изменение периода выборки данных

В функции `weekly_retraining()` измените параметр `days_back`:

```python
result = retrain_model(db, days_back=14)  # 14 дней вместо 7
```

## Мониторинг

### Логи

Система выводит подробные логи:

```
🔄 Запуск еженедельного дообучения модели...
📅 Сбор данных за период: 2024-01-01 - 2024-01-08
✅ Найдено новых транзакций: 150
📊 Распределение категорий:
Food       45
Misc       30
Rent       25
Salary     20
Shopping   30
🚀 Начало обучения модели...
📊 Данных для обучения: 700
🎯 Обучение модели...
✅ Модель обучена и сохранена в: app/ml/classifier_v2.pkl
✅ Дообучение завершено успешно. Новых транзакций: 150
```

### Проверка статуса

Проверьте, что планировщик запущен:

```python
from apscheduler.schedulers.asyncio import AsyncIOScheduler

# В логах при запуске должно быть:
# ✅ Планировщик задач запущен. Дообучение модели запланировано на каждый понедельник в 3:00
```

## Требования

### Зависимости

- `apscheduler==3.10.4` - планировщик задач
- `lightgbm==4.1.0` - модель машинного обучения
- `joblib==1.3.2` - сохранение/загрузка модели
- `pandas` - работа с данными
- `numpy` - численные операции

### Данные

- Транзакции должны быть в БД с корректными категориями
- Оригинальный CSV файл (опционально) для объединения данных

## Устранение неполадок

### Модель не дообучается

1. Проверьте логи приложения
2. Убедитесь, что есть новые транзакции в БД
3. Проверьте, что планировщик запущен (логи при старте)

### Ошибка при дообучении

1. Проверьте формат данных в БД
2. Убедитесь, что категории корректно маппятся
3. Проверьте доступность оригинального CSV файла (если используется)

### Низкое качество модели после дообучения

1. Увеличьте период выборки данных (`days_back`)
2. Проверьте качество данных в БД
3. Убедитесь, что достаточно данных для обучения

## Безопасность

- Endpoint `/ml/retrain` требует авторизации
- Дообучение использует данные всех пользователей (анонимизированные)
- Модель сохраняется локально в `app/ml/classifier_v2.pkl`

## Примеры использования

### Ручной запуск через API

```python
import requests

token = "YOUR_BEARER_TOKEN"
response = requests.post(
    "http://localhost:8000/ml/retrain",
    params={"days_back": 14},
    headers={"Authorization": f"Bearer {token}"}
)
print(response.json())
```

### Проверка метрик

После дообучения проверьте метрики в ответе API или в логах.

## Дополнительная информация

- Модель использует LightGBM с fallback правилами
- Обучение происходит на объединенных данных (оригинал + новые)
- Модель автоматически перезагружается после дообучения


