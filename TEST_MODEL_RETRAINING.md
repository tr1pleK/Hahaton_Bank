# Руководство по тестированию дообучения модели

Это руководство поможет вам проверить, что модель действительно дообучается на новых данных.

## Способы тестирования

### 1. Проверка через API endpoint (Рекомендуемый способ)

#### Шаг 1: Получите токен авторизации

```bash
# Войдите в систему
curl -X POST "http://localhost:8000/auth/login" \
  -H "Content-Type: application/json" \
  -d '{
    "email": "your_email@example.com",
    "password": "your_password"
  }'
```

Сохраните `access_token` из ответа.

#### Шаг 2: Проверьте метрики модели ДО дообучения

Сначала создайте несколько тестовых транзакций с категориями:

```bash
# Создайте транзакции с разными категориями
curl -X POST "http://localhost:8000/transactions" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "date": "2024-12-01",
    "amount": 5000,
    "description": "Аренда квартиры",
    "category": "UTILITIES"
  }'

curl -X POST "http://localhost:8000/transactions" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "date": "2024-12-02",
    "amount": 1500,
    "description": "Покупка продуктов",
    "category": "PRODUCTS"
  }'
```

#### Шаг 3: Запустите дообучение

```bash
curl -X POST "http://localhost:8000/ml/retrain?days_back=7" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

#### Шаг 4: Проверьте ответ

Ответ должен содержать:
```json
{
  "success": true,
  "message": "Модель успешно дообучена",
  "new_transactions_count": 2,
  "metrics": {
    "f1_weighted": 0.85,
    "f1_macro": 0.82,
    "train_size": 100,
    "test_size": 43,
    "total_samples": 143,
    "categories": ["Food", "Misc", "Rent", "Salary", "Shopping"]
  },
  "model_path": "/app/app/ml/classifier_v2.pkl"
}
```

**Важно:** Обратите внимание на:
- `new_transactions_count` - должно быть больше 0
- `metrics.f1_weighted` и `metrics.f1_macro` - метрики качества модели
- `metrics.total_samples` - общее количество данных для обучения

### 2. Проверка времени изменения файла модели

#### Шаг 1: Запомните время последнего изменения файла модели

```bash
# В контейнере или локально
ls -lh app/ml/classifier_v2.pkl
stat app/ml/classifier_v2.pkl  # Linux/Mac
```

Запишите время модификации.

#### Шаг 2: Запустите дообучение

```bash
curl -X POST "http://localhost:8000/ml/retrain?days_back=7" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

#### Шаг 3: Проверьте, что файл обновился

```bash
ls -lh app/ml/classifier_v2.pkl
stat app/ml/classifier_v2.pkl
```

Время модификации должно измениться на текущее время.

### 3. Сравнение предсказаний до и после дообучения

#### Шаг 1: Получите предсказание ДО дообучения

```bash
curl -X POST "http://localhost:8000/transactions/predict" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '[
    {
      "date": "2024-12-10",
      "description": "Покупка продуктов в магазине",
      "withdrawal": 2500,
      "deposit": 0,
      "balance": 10000
    }
  ]'
```

Запишите предсказанную категорию и вероятность.

#### Шаг 2: Создайте транзакцию с правильной категорией

```bash
curl -X POST "http://localhost:8000/transactions" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "date": "2024-12-10",
    "amount": 2500,
    "description": "Покупка продуктов в магазине",
    "category": "PRODUCTS"
  }'
```

#### Шаг 3: Запустите дообучение

```bash
curl -X POST "http://localhost:8000/ml/retrain?days_back=7" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

#### Шаг 4: Получите предсказание ПОСЛЕ дообучения

```bash
curl -X POST "http://localhost:8000/transactions/predict" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '[
    {
      "date": "2024-12-10",
      "description": "Покупка продуктов в магазине",
      "withdrawal": 2500,
      "deposit": 0,
      "balance": 10000
    }
  ]'
```

**Ожидаемый результат:** 
- Если модель дообучилась правильно, предсказание должно стать более точным
- Вероятность предсказания может измениться
- Категория может измениться, если модель улучшилась

### 4. Проверка через логи приложения

#### Шаг 1: Запустите приложение с выводом логов

```bash
# В контейнере
docker logs -f your_container_name

# Или локально
python -m uvicorn app.main:app --reload
```

#### Шаг 2: Запустите дообучение

```bash
curl -X POST "http://localhost:8000/ml/retrain?days_back=7" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

#### Шаг 3: Проверьте логи

Вы должны увидеть в логах:
```
🔄 Начало дообучения модели...
📅 Сбор данных за период: 2024-12-01 - 2024-12-08
✅ Найдено новых транзакций: 5
📊 Распределение категорий:
Category
Food      2
Rent      1
Shopping  2
🚀 Начало обучения модели...
📊 Данных для обучения: 150
📊 Train size: 105, Test size: 45
🎯 Обучение модели...
=== Метрики модели ===
              precision    recall  f1-score   support
       Food       0.95      0.92      0.93        12
       Misc       0.88      0.90      0.89        10
       Rent       1.00      1.00      1.00         8
     Salary       1.00      1.00      1.00         7
   Shopping       0.92      0.88      0.90         8
    accuracy                           0.93        45
   macro avg       0.95      0.94      0.94        45
weighted avg       0.93      0.93      0.93        45

F1 (weighted): 0.9300
F1 (macro): 0.9400
✅ Модель обучена и сохранена в: /app/app/ml/classifier_v2.pkl
```

### 5. Автоматизированный тест (Python скрипт)

Создайте файл `test_retraining.py`:

```python
import requests
import time
import os
from pathlib import Path

BASE_URL = "http://localhost:8000"
EMAIL = "your_email@example.com"
PASSWORD = "your_password"

# 1. Авторизация
login_response = requests.post(
    f"{BASE_URL}/auth/login",
    json={"email": EMAIL, "password": PASSWORD}
)
token = login_response.json()["access_token"]
headers = {"Authorization": f"Bearer {token}"}

# 2. Проверка времени модификации модели ДО
model_path = Path("app/ml/classifier_v2.pkl")
if model_path.exists():
    mtime_before = model_path.stat().st_mtime
    print(f"Время модификации модели ДО: {time.ctime(mtime_before)}")
else:
    mtime_before = None
    print("Модель не найдена")

# 3. Создание тестовых транзакций
print("\n📝 Создание тестовых транзакций...")
transactions = [
    {
        "date": "2024-12-01",
        "amount": 5000,
        "description": "Аренда квартиры",
        "category": "UTILITIES"
    },
    {
        "date": "2024-12-02",
        "amount": 1500,
        "description": "Покупка продуктов",
        "category": "PRODUCTS"
    }
]

for txn in transactions:
    response = requests.post(
        f"{BASE_URL}/transactions",
        json=txn,
        headers=headers
    )
    print(f"  ✅ Транзакция создана: {response.status_code}")

# 4. Запуск дообучения
print("\n🔄 Запуск дообучения...")
retrain_response = requests.post(
    f"{BASE_URL}/ml/retrain?days_back=7",
    headers=headers
)
result = retrain_response.json()
print(f"  Результат: {result}")

# 5. Проверка времени модификации модели ПОСЛЕ
if model_path.exists():
    mtime_after = model_path.stat().st_mtime
    print(f"\nВремя модификации модели ПОСЛЕ: {time.ctime(mtime_after)}")
    
    if mtime_before and mtime_after > mtime_before:
        print("✅ Модель была обновлена!")
    elif mtime_before is None:
        print("✅ Модель была создана!")
    else:
        print("⚠️ Время модификации не изменилось")
else:
    print("❌ Модель не найдена после дообучения")

# 6. Проверка метрик
if result.get("success"):
    metrics = result.get("metrics", {})
    print(f"\n📊 Метрики модели:")
    print(f"  F1 (weighted): {metrics.get('f1_weighted', 'N/A')}")
    print(f"  F1 (macro): {metrics.get('f1_macro', 'N/A')}")
    print(f"  Новых транзакций: {result.get('new_transactions_count', 0)}")
    print(f"  Всего данных: {metrics.get('total_samples', 0)}")
else:
    print(f"\n❌ Дообучение не удалось: {result.get('message', 'Unknown error')}")

print("\n✅ Тест завершен!")
```

Запустите скрипт:
```bash
python test_retraining.py
```

## Критерии успешного тестирования

✅ **Модель дообучается успешно, если:**

1. **API возвращает успешный ответ:**
   - `success: true`
   - `new_transactions_count > 0`
   - Метрики присутствуют в ответе

2. **Файл модели обновляется:**
   - Время модификации файла изменяется
   - Размер файла может измениться

3. **Метрики модели разумные:**
   - `f1_weighted` и `f1_macro` в диапазоне 0.7-1.0
   - `total_samples` больше 0

4. **Логи показывают процесс:**
   - Видны этапы дообучения
   - Показаны метрики
   - Нет критических ошибок

5. **Предсказания улучшаются (опционально):**
   - После дообучения модель лучше предсказывает категории
   - Вероятности предсказаний становятся выше

## Возможные проблемы

### Проблема: `new_transactions_count: 0`

**Причина:** Нет новых транзакций за указанный период.

**Решение:**
- Создайте транзакции с датами в пределах `days_back`
- Увеличьте `days_back` параметр

### Проблема: Модель не обновляется

**Причина:** Ошибка при сохранении модели или недостаточно данных.

**Решение:**
- Проверьте права доступа к файлу модели
- Убедитесь, что есть минимум 10 транзакций для дообучения
- Проверьте логи на наличие ошибок

### Проблема: Низкие метрики

**Причина:** Недостаточно данных или данные низкого качества.

**Решение:**
- Добавьте больше транзакций с правильными категориями
- Убедитесь, что категории соответствуют ожидаемым: Food, Misc, Rent, Salary, Shopping

## Дополнительные проверки

### Проверка количества деревьев в модели

```python
import joblib
from pathlib import Path

model_path = Path("app/ml/classifier_v2.pkl")
if model_path.exists():
    model = joblib.load(model_path)
    print(f"Количество деревьев: {model.n_estimators}")
    print(f"Количество признаков: {model.n_features_in_}")
```

### Проверка распределения категорий в данных

```python
from app.database import SessionLocal
from app.models.transaction import Transaction
from datetime import date, timedelta
from collections import Counter

db = SessionLocal()
end_date = date.today()
start_date = end_date - timedelta(days=7)

transactions = db.query(Transaction).filter(
    Transaction.date >= start_date,
    Transaction.date <= end_date
).all()

categories = [txn.category.value for txn in transactions]
print("Распределение категорий:")
print(Counter(categories))
```

## Заключение

Используйте комбинацию этих методов для полной проверки дообучения модели. Рекомендуется начать с проверки через API endpoint и логов, затем проверить время модификации файла модели.

